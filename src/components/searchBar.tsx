import * as React from 'react'

const SearchBar: React.FC = () => {
    return (
        <div>
            <div>
                <input type="text" />
                <button>クリック</button>
            </div>
        </div>
    )
}

export default SearchBar